{
  "id": "sub-dev",
  "label": "Sub-dev (Supabase MCP denied)",
  "description": "Full-stack sub-developer under the tech-lead. Permissive like default, but the account-level Supabase MCP is denied -- no direct prod-Supabase write/read. DB work = migration file -> the applier-pool (tech-lead/PM) reviews + applies. The applier-pool is NOT on this profile.",
  "permissionMode": "permissive",
  "filesystem": {
    "allow": [],
    "deny": [
      "Read(${HOME}/.git-credentials)",
      "Read(**/.git-credentials)",
      "Read(${HOME}/.config/git/credentials)",
      "Read(${HOME}/.env)",
      "Read(**/.env)",
      "mcp__claude_ai_Supabase__*"
    ]
  }
}